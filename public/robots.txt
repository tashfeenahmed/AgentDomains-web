# agentdomains.co — free domains for the sites and APIs AI agents build.
# This site is built for agents, so every crawler is welcome: no disallows.
#
# makes.fyi no longer serves this site: the apex Worker answers it with a 301 to
# the same path on agentdomains.co, which is the canonical brand host and the one
# we want indexed. (Claimed *.makes.fyi subdomains are unaffected — they are
# served by the forward/proxy Workers and keep working.) A crawler that asks
# makes.fyi for this file is redirected here, so there is one robots.txt and one
# sitemap, both naming agentdomains.co.

User-agent: *
Allow: /

Sitemap: https://agentdomains.co/sitemap.xml
